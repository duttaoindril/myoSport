"use strict";

var express = require("express");
var dtw = require("dtw");
var firebase = require("firebase");
var myo = require("myo");
var jade = require("jade");

var app = express();
app.listen(3000);